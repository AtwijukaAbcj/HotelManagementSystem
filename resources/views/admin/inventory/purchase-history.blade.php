<!DOCTYPE html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>Purchase Order History</title>@include('admin.css')<style>.history-page{background:#f6f8fb;min-height:100vh}.history-page .page-wrapper{padding-top:80px}.history-container{max-width:1100px;margin:0 auto;padding:30px 28px 56px}.history-card{border:1px solid #e7ebf1;border-radius:12px;box-shadow:0 8px 22px rgba(15,23,42,.05)}@media(max-width:767px){.history-page .page-wrapper{padding-top:72px}.history-container{padding:24px 16px 40px}}</style></head>
<body><div class="main-wrapper history-page">@include('admin.header')@include('admin.sidebar')<div class="page-wrapper"><main class="history-container"><div class="d-flex justify-content-between align-items-start mb-4"><div><h3 class="page-title mb-1">{{ $purchase->po_number ?: '#'.$purchase->id }}</h3><p class="text-muted mb-0">Approval and status transition history.</p></div><a href="{{ route('purchases.index') }}" class="btn btn-outline-secondary">Back to orders</a></div><div class="card history-card mb-4"><div class="card-body"><div class="row"><div class="col-md-3"><small class="text-muted d-block">Supplier</small><strong>{{ $purchase->supplier }}</strong></div><div class="col-md-3"><small class="text-muted d-block">Current status</small><strong>{{ ucwords(str_replace('_', ' ', $purchase->status)) }}</strong></div><div class="col-md-3"><small class="text-muted d-block">Total</small><strong>UGX {{ number_format($purchase->display_total, 0) }}</strong></div><div class="col-md-3"><small class="text-muted d-block">Created</small><strong>{{ $purchase->created_at->format('d M Y H:i') }}</strong></div></div></div></div><div class="card history-card"><div class="card-body p-0"><div class="table-responsive"><table class="table table-hover mb-0"><thead class="thead-light"><tr><th>Date</th><th>Action</th><th>Transition</th><th>By</th><th>Comment</th></tr></thead><tbody>@forelse($purchase->statusHistory as $history)<tr><td>{{ $history->created_at->format('d M Y H:i') }}</td><td>{{ ucwords(str_replace('_', ' ', $history->action)) }}</td><td>{{ $history->from_status ? ucwords(str_replace('_', ' ', $history->from_status)) : 'New' }} &rarr; {{ ucwords(str_replace('_', ' ', $history->to_status)) }}</td><td>{{ $history->user->name ?? 'System' }}</td><td>{{ $history->comment ?: '-' }}</td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">No status history recorded yet.</td></tr>@endforelse</tbody></table></div></div></div></main></div></div>@include('admin.script')</body></html>
