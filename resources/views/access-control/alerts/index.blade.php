<x-layouts.access-control :title="'Security Alerts'"><div><a href="{{ route('access-control.index') }}" class="text-sm font-semibold text-teal-700">← Security Center</a><h1 class="mt-2 text-2xl font-bold">Security Alerts</h1><p class="mt-1 text-sm text-slate-500">Review denied access, repeated failures, and suspicious activity.</p></div><div class="mt-6 overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr><th class="px-5 py-3">Alert</th><th class="px-5 py-3">Severity</th><th class="px-5 py-3">Location</th><th class="px-5 py-3">Status</th><th class="px-5 py-3">Date</th><th class="px-5 py-3">Resolution</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($alerts as $alert)<tr class="hover:bg-slate-50"><td class="px-5 py-4 font-semibold">{{ ucwords(str_replace('_', ' ', $alert->alert_type)) }}<small class="block text-slate-500">{{ $alert->description }}</small></td><td class="px-5 py-4"><x-access-status-badge :status="$alert->severity" /></td><td class="px-5 py-4">{{ $alert->location ?: 'Unknown' }}</td><td class="px-5 py-4"><x-access-status-badge :status="$alert->status" /></td><td class="px-5 py-4 text-slate-500">{{ $alert->created_at->format('d M Y H:i') }}</td><td class="px-5 py-4">@if($alert->status !== 'resolved')<form method="POST" action="{{ route('access-control.alerts.update', $alert) }}" class="flex gap-2">@csrf<select name="status" class="rounded-lg border-slate-200 text-xs"><option value="investigating">Investigating</option><option value="resolved">Resolved</option></select><button class="text-xs font-bold text-teal-700">Save</button></form>@else<span class="text-xs text-slate-500">Resolved {{ optional($alert->resolved_at)->format('d M Y') }}</span>@endif</td></tr>@empty<tr><td colspan="6" class="px-5 py-12 text-center text-slate-500">No security alerts have been recorded.</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-100 px-5 py-4">{{ $alerts->links() }}</div></div></x-layouts.access-control>
