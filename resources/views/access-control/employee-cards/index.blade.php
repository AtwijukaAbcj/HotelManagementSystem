<x-layouts.access-control :title="'Employee Mastercards'"><div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><a href="{{ route('access-control.index') }}" class="text-sm font-semibold text-teal-700">← Security Center</a><h1 class="mt-2 text-2xl font-bold">Employee Mastercards</h1><p class="mt-1 text-sm text-slate-500">Manage staff credentials for authorized work areas.</p></div><a href="{{ route('access-control.employee-cards.create') }}" class="rounded-xl bg-teal-600 px-4 py-2.5 text-sm font-bold text-white hover:bg-teal-700">+ Issue employee card</a></div><form class="mt-6 flex gap-3 rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><input name="search" value="{{ request('search') }}" class="flex-1 rounded-lg border-slate-200 text-sm focus:border-teal-500 focus:ring-teal-500" placeholder="Search employee or card"><button class="rounded-lg bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Search</button></form><div class="mt-5 overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr><th class="px-5 py-3">Employee</th><th class="px-5 py-3">Department</th><th class="px-5 py-3">Card</th><th class="px-5 py-3">Permitted areas</th><th class="px-5 py-3">Status</th><th class="px-5 py-3 text-right">Action</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($cards as $card)<tr class="hover:bg-slate-50"><td class="px-5 py-4 font-semibold">{{ $card->user?->name ?? 'Unassigned' }}<small class="block text-slate-500">{{ $card->user?->role }}</small></td><td class="px-5 py-4">{{ $card->department ?: 'Hotel operations' }}</td><td class="px-5 py-4">{{ $card->card_number }}</td><td class="px-5 py-4 text-slate-500">{{ implode(', ', $card->permitted_areas ?: ['Assigned areas']) }}</td><td class="px-5 py-4"><x-access-status-badge :status="$card->status" /></td><td class="px-5 py-4 text-right">@if($card->status === 'active')<form method="POST" action="{{ route('access-control.cards.suspend', $card) }}">@csrf<button class="text-xs font-semibold text-amber-700">Suspend</button></form>@else<form method="POST" action="{{ route('access-control.cards.activate', $card) }}">@csrf<button class="text-xs font-semibold text-emerald-700">Activate</button></form>@endif</td></tr>@empty<tr><td colspan="6" class="px-5 py-12 text-center text-slate-500">No employee Mastercards have been issued.</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-100 px-5 py-4">{{ $cards->links() }}</div></div></x-layouts.access-control>
