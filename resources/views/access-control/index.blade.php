<x-layouts.access-control :title="'Security & Access Center'">
    <div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-sm font-semibold text-teal-700">Security operations</p><h1 class="mt-1 text-3xl font-bold tracking-tight text-slate-950">Security & Access Center</h1><p class="mt-2 max-w-2xl text-sm text-slate-500">Manage guest access, employee permissions, access points and security activity.</p></div><div class="text-left text-sm text-slate-500 sm:text-right"><div>{{ now()->format('l, d M Y') }}</div><div class="mt-1 text-xs">Synced {{ now()->format('H:i') }}</div></div></div>
    @if(session('message'))<div class="mt-6 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('message') }}</div>@endif
    <section class="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><x-access-stat label="Active cards" :value="$summary['active_cards']" hint="Guest and employee cards" /><x-access-stat label="Denied access today" :value="$summary['denied_today']" hint="Review security events" tone="red" /><x-access-stat label="Unresolved alerts" :value="$summary['unresolved_alerts']" hint="Open and investigating" tone="amber" /><x-access-stat label="Offline access points" :value="$summary['offline_points']" hint="Needs attention" tone="red" /></section>
    <section class="mt-8"><div class="mb-4 flex items-end justify-between"><div><h2 class="text-lg font-bold text-slate-900">Security modules</h2><p class="mt-1 text-sm text-slate-500">Choose an area to manage.</p></div></div><div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">@foreach($modules as $module)<x-access-module-card :module="$module" />@endforeach</div></section>
    <section class="mt-8 grid gap-5 lg:grid-cols-2"><div class="rounded-xl border border-slate-200 bg-white shadow-sm"><div class="flex items-center justify-between border-b border-slate-100 px-5 py-4"><div><h2 class="font-bold">Recent security alerts</h2><p class="text-xs text-slate-500">Latest events requiring attention</p></div><a href="{{ route('access-control.alerts.index') }}" class="text-sm font-semibold text-teal-700 hover:text-teal-900">View all</a></div><div class="divide-y divide-slate-100">@forelse($recentAlerts as $alert)<a href="{{ route('access-control.alerts.index') }}" class="block px-5 py-4 transition hover:bg-slate-50"><div class="flex items-center justify-between gap-3"><span class="font-semibold text-slate-800">{{ ucwords(str_replace('_', ' ', $alert->alert_type)) }}</span><x-access-status-badge :status="$alert->severity" /></div><p class="mt-1 text-sm text-slate-500">{{ $alert->location ?: 'Unknown location' }} · {{ $alert->created_at->diffForHumans() }}</p></a>@empty<div class="px-5 py-8 text-center text-sm text-slate-500">No security alerts have been recorded.</div>@endforelse</div></div><div class="rounded-xl border border-slate-200 bg-white shadow-sm"><div class="flex items-center justify-between border-b border-slate-100 px-5 py-4"><div><h2 class="font-bold">Recent access activity</h2><p class="text-xs text-slate-500">Latest reader activity</p></div><a href="{{ route('access-control.events.index') }}" class="text-sm font-semibold text-teal-700 hover:text-teal-900">Review events</a></div><div class="divide-y divide-slate-100">@forelse($recentEvents as $event)<div class="flex items-center justify-between gap-3 px-5 py-3"><div><div class="text-sm font-semibold text-slate-800">{{ $event->guest?->full_name ?? $event->user?->name ?? 'Unknown cardholder' }}</div><div class="text-xs text-slate-500">{{ ucfirst($event->event_type) }} · {{ $event->area }} · {{ $event->accessed_at->format('d M H:i') }}</div></div><x-access-status-badge :status="$event->result" /></div>@empty<div class="px-5 py-8 text-center text-sm text-slate-500">No access activity has been recorded.</div>@endforelse</div></div></section>
</x-layouts.access-control>
