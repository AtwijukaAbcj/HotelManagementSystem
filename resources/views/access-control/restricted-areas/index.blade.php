<x-layouts.access-control :title="'Restricted Areas'"><div><a href="{{ route('access-control.index') }}" class="text-sm font-semibold text-teal-700">← Security Center</a><h1 class="mt-2 text-2xl font-bold">Restricted Areas</h1><p class="mt-1 text-sm text-slate-500">Secure areas that require employee authorization.</p></div>@if(session('message'))<div class="mt-4 rounded-xl bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('message') }}</div>@endif<div class="mt-6 grid gap-5 lg:grid-cols-3"><form method="POST" action="{{ route('access-control.restricted-areas.store') }}" class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">@csrf<h2 class="font-bold">Add controlled area</h2><input name="name" required class="mt-4 w-full rounded-lg border-slate-200 text-sm" placeholder="Server room"><textarea name="description" class="mt-3 w-full rounded-lg border-slate-200 text-sm" rows="3" placeholder="Purpose and access notes"></textarea><select name="severity" class="mt-3 w-full rounded-lg border-slate-200 text-sm"><option value="warning">Warning</option><option value="high">High</option><option value="critical">Critical</option></select><button class="mt-4 w-full rounded-lg bg-teal-600 px-4 py-2 text-sm font-bold text-white">Add area</button></form><div class="grid gap-4 sm:grid-cols-2 lg:col-span-2">@forelse($areas as $area)<div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex items-center justify-between"><h2 class="font-bold">{{ $area->name }}</h2><x-access-status-badge :status="$area->is_active ? 'active' : 'disabled'" /></div><p class="mt-2 text-sm text-slate-500">{{ $area->description ?: 'Controlled hotel area' }}</p><p class="mt-4 text-xs font-semibold uppercase tracking-wider text-slate-400">{{ ucfirst($area->severity) }} security</p></div>@empty<div class="rounded-xl border border-dashed border-slate-300 bg-white px-6 py-12 text-center text-slate-500 sm:col-span-2">No restricted areas have been configured.</div>@endforelse</div></div></x-layouts.access-control>
